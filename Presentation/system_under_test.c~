#include <stdio.h>
#include "system_under_test.h"

//void set_sex(struct person, enum SEX);
void LPP_internalSetHwTimerCallback(U16 id, void (*timerCallback_func)(void));

int function_to_test()
{
  struct person x;
  x.first_name ="Luca";
  x.last_name ="Ciavaglia";
  x.age = 23; 
  set_sex(x, MALE);

  return 1;
}
